## Summary

- What changed?
- Why?

## Testing

- [ ] `PYTHONPATH=src python -m unittest discover -s tests -p "test_*.py" -v`
- [ ] Manual verification (if UI behavior changed)

## Checklist

- [ ] Docs updated (if user-facing behavior changed)
- [ ] No secrets or local paths committed
